# api/는 feature/integration2(백엔드)를 로컬에서 같이 보려고 별도로 clone해둔 완전히 독립된 저장소임
# (자체 .git 있음, 이 저장소의 브랜치/커밋과 무관). feature/ui엔 항상 무시되어야 함.
# 이름을 api/로 맞춘 이유: feature/integration2 브랜치의 최상위 구조가 실제로 api/ 하나뿐이라,
# 나중에 main에 병합됐을 때의 최종 구조(web/frontend/ + api/ + docs/)와 로컬 경로를 일치시키기 위함.
/api/
